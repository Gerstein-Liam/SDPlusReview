<UserControl x:Class="WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views"
             mc:Ignorable="d" 
                     xmlns:viewmodels="clr-namespace:WPF.ViewModels"
                d:DataContext="{d:DesignInstance viewmodels:SettingsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="WhiteSmoke">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Background="{StaticResource BrushPrimary2}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0"  Grid.Column="0" Content="Travailler hors connexion" />
            <CheckBox Grid.Row="0" Grid.Column="1" IsChecked="{Binding WorkWithoutConnection }"  VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Label Grid.Row="1" Grid.Column="0" Content="Activer mode dépannage"/>
            <CheckBox Grid.Row="1" Grid.Column="1" IsChecked="{Binding EnableDebuggingMode }" VerticalAlignment="Center" HorizontalAlignment="Center"/>

        </Grid>
    </Grid>
</UserControl>
