<UserControl x:Class="WPF.Views.DashboardItems.SearchAdressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF.Views.DashboardItems"
             xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
                 xmlns:viewmodels="clr-namespace:WPF.ViewModels.DashboardItems"
     mc:Ignorable="d"
     d:DataContext="{d:DesignInstance viewmodels:SearchAdressViewModel}"
             d:DesignHeight="900" d:DesignWidth="500">




    <Border Background="White" CornerRadius="10" BorderThickness="0.5" Opacity="0.95" BorderBrush="{StaticResource BrushPrimary2}" >
        <Grid HorizontalAlignment="Stretch"  Panel.ZIndex="2" Margin="10 10 10 10"  x:Name="ListContainer">
            <Grid.RowDefinitions >
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
            </Grid>
            <Grid Grid.Row="1" Margin="0 0 0 10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="AUTO"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>

                </Grid.ColumnDefinitions>
                <Label Content="ADRESSES" HorizontalAlignment="Left"  VerticalAlignment="Center"/>
                <TextBox  Grid.Column="1"  Text="{Binding AdressToSearch,UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >

                    <TextBox.InputBindings>
                        <KeyBinding Command="{Binding SearchAdressDBCommandBt}" Key="Enter" />
                    </TextBox.InputBindings>

                </TextBox>
                <Button  Content="{materialIcons:MaterialIconExt Kind=Search}" Command="{Binding SearchAdressDBCommandBt}"  Grid.Column="2"  HorizontalAlignment="Right"/>

            </Grid>
            <ListView Grid.Row="2" ItemsSource="{Binding SearchResults}" ScrollViewer.HorizontalScrollBarVisibility="Hidden"  SelectedItem="{Binding SelectedAdress}"   >
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <Border  Width="{Binding ActualWidth,ElementName=AdressListContainer}" Margin="10">
                            <Grid  >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" FontSize="10" VerticalAlignment="Center" Text="{Binding Adress}"/>
                                <Label Content="|" Grid.Column="1"/>
                                <TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Stretch"  FontSize="10" FontStyle="Italic" Text="{Binding ZIP}"/>
                                <!--<TextBox Grid.Row="1" Text="{Binding CoordinatesAsString ,Mode=OneWay}"/>-->
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>



</UserControl>