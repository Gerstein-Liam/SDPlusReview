<UserControl x:Class="WPF.Views.DashboardItems.OwnerEdit"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        
             xmlns:local="clr-namespace:WPF.ViewModels.DashboardItems"
         xmlns:converters="clr-namespace:WPF.Converters"
         xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:OwnerEditViewModel}" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border CornerRadius="10" BorderThickness="0.5" BorderBrush="{StaticResource BrushPrimary2}" Background="White" Padding="20">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="AUTO"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Label Content="NOM" Grid.Row="0"/>
                <TextBox Width="150" Text="{Binding OwnerName,UpdateSourceTrigger=PropertyChanged}" Grid.Column="1" Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Left" />
                <!--<Label Content="ADRESSES" Grid.Row="1"/>
                <TextBox Width="150"  Grid.Column="1" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left" />-->
                <Button Content="{materialIcons:MaterialIconExt Kind=ContentSave}" Height="30" HorizontalAlignment="Stretch" Grid.ColumnSpan="2" Command="{Binding ValidedBt}" Grid.Row="2" Grid.Column="1" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
